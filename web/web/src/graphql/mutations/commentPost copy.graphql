mutation DeleteSub($id: String!, $subid: String!) {
    deleteSub(id: $id, subid: $subid) {
        errors {
            field
            message
        }
        post {
            id
            author
        }
    }
}