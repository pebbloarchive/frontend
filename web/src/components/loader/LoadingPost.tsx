import React from "react";

interface Props {
    onClick?: any;
    isLoading?: boolean;
    isDisabled?: boolean;
}

export const LoadingPost: React.FC<Props> = ({
    onClick,
    isLoading,
    isDisabled
}) => {
    <>
        
    </>
}