import styles from '../../styles/components/whypebblo.module.css';
import Loader from '../../components/Loader';

const Page = () => {
return (
    <div>
        <Loader />
    </div>
)
}

export default Page;