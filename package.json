{
  "name": "frontend",
  "private": true,
  "scripts": {
    "dev": "lerna bootstrap && lerna run build && lerna run dev --parallel"
  },
  "workspaces": [
    "core",
    "web"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {
    "npm": "^6.14.8"
  }
}
